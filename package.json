{
  "name": "cobweb-research",
  "version": "0.1.0",
  "description": "蛛网图谱 · 研究仓库 (方法论 + 状态机 + 核心算法)",
  "main": "core/index.ts",
  "types": "core/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "keywords": ["cobweb", "kernel-state", "force-directed", "self-awareness", "ai-companion"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
